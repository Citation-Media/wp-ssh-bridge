```bash title="Install the latest release"
# Detects your OS and CPU, verifies the SHA-256 checksum, and unpacks the binary.
# Inside a DDEV project it lands in .ddev/bin, otherwise in the current directory.
curl -fsSL https://wp-ssh-bridge.citation.media/install.sh | sh
```
